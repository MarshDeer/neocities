name: Build site
on:
  push:
    branches:
      - deer
  pull_request:
    branches:
      - deer

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Dependencies
        uses: BSFishy/pip-action@v3
        with:
          packages: autosite

      - name: Autosite
        run: autosite -a

      - name: Deploy
        uses: bcomnes/deploy-to-neocities@v3
        with:
          api_token: ${{ secrets.APIKEY }}
          dist_dir: out
          cleanup: true
